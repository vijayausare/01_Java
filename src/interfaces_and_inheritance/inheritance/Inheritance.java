package interfaces_and_inheritance.inheritance;

public class Inheritance {

	/*
	 * 1. Excepting Object, which has no superclass, every class has one and only
	 * 	  one direct superclass (single inheritance). 
	 * 
	 * 2. In the absence of any other explicit superclass, every class is implicitly a subclass of Object.
	 */
}
